명령 프롬프트 명령어(git)

d:		[D드라이브로 이동]
c:		[C드라이브로 이동]

git --version	[git 버젼 확인]
git clone (주소)	[(주소)의 파일을 현재 위치에 저장]
git add (파일)	[(파일)을 추가한다]
git reset (파일)	[(파일)을 제거한다]
git commit	[추가한 파일을 저장한다]
	-m ""	[추가한 파일에 ""안의 메모를 붙인다]
git push		[저장한 파일을 github에 올린다]
git status		[깃 내의 변화를 파악한다]
